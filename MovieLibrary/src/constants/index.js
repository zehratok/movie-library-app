import {apiUrl} from './api';
import theme, {COLORS, SIZES, FONTS} from './theme';
import fakeData, {categories, userProfile} from './fakeData';

export default {
  apiUrl,
  categories,
  COLORS,
  fakeData,
  FONTS,
  SIZES,
  theme,
  userProfile,
};
